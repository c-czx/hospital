<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.DepartmentMapper">
    <resultMap id="DepartmentResultMap" type="com.hospital.entity.Department">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="description" property="description" />
        <result column="status" property="status" />
    </resultMap>

    <insert id="insert" parameterType="com.hospital.entity.Department">
        INSERT INTO departments (name, description, created_at)
        VALUES (#{name}, #{description}, #{createdAt})
    </insert>

    <update id="update" parameterType="com.hospital.entity.Department">
        UPDATE departments
        SET name = #{name}, description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM departments WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Long" resultMap="DepartmentResultMap">
        SELECT * FROM departments WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="DepartmentResultMap">
        SELECT * FROM departments
    </select>
</mapper>